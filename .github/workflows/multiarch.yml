name: Docker PostGIS CI

on:
  push:
    paths-ignore:
      - '.github/dependabot.yml'
      - '.gitignore'
      - 'LICENSE'
      - 'README.md'
  pull_request:
  schedule:
    - cron: '15 5 * * 2'  # Weekly, 1 day after the upstream

# Cancel existing executions when new commits are pushed onto the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:

  make-docker-images:

    strategy:
      fail-fast: false
      matrix:
        # exclude the latest version set for use with "include" below
        postgres: [13, 14, 15, 16, 17]
        postgis: ['3.5']
        latest: [false]
        include:
        - postgres: 18  # only use the single latest version with "latest: true"
          postgis: '3.6'
          latest: true

    name: Build docker image for ${{ matrix.postgres }}-${{ matrix.postgis }}

    runs-on: ubuntu-24.04

    permissions:
      contents: read
      id-token: write
      packages: write

    env:
      VERSION: ${{ matrix.postgres }}-${{ matrix.postgis }}

    steps:

    - name: Set up Depot CLI
      uses: depot/setup-action@15c09a5f77a0840ad4bce955686522a257853461

    - name: Docker meta
      id: meta
      uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051
      with:
        images: ghcr.io/${{ github.repository_owner }}/postgis
        labels: |
          org.opencontainers.image.title=postgis
          org.opencontainers.image.version=${{ env.VERSION }}
          org.opencontainers.image.base.name=docker.io/postgis/postgis:${{ env.VERSION }}
        tags: |
          type=raw,value=${{ env.VERSION }},enable=true,priority=220
          type=raw,value=${{ matrix.postgres }},enable=true,priority=210
          type=raw,value=latest,enable=${{ matrix.latest }},priority=200

    - name: Login to GitHub Container Registry
      if: ${{ (github.ref == 'refs/heads/multiarch') && (github.event_name != 'pull_request') && !env.ACT }}
      uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Multi-platform build without push
      if: ${{ !((github.ref == 'refs/heads/multiarch') && (github.event_name != 'pull_request') && !env.ACT) }}
      uses: depot/build-push-action@5f3b3c2e5a00f0093de47f657aeaefcedff27d18
      with:
        project: p4q8k8mb22
        platforms: linux/amd64,linux/arm64
        context: https://github.com/postgis/docker-postgis.git#master
        file: ${{ env.VERSION }}/Dockerfile
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        pull: true
        push: false

    - name: Multi-platform build and push
      if: ${{ (github.ref == 'refs/heads/multiarch') && (github.event_name != 'pull_request') && !env.ACT }}
      uses: depot/build-push-action@5f3b3c2e5a00f0093de47f657aeaefcedff27d18
      with:
        project: p4q8k8mb22
        no-cache: true
        platforms: linux/amd64,linux/arm64
        context: https://github.com/postgis/docker-postgis.git#master
        file: ${{ env.VERSION }}/Dockerfile
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        pull: true
        push: true
